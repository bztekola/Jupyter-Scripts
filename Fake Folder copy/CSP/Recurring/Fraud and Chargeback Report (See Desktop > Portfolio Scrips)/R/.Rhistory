weights <- chi.squared(params, data = train)
#install.packages('rJava')
#library(rJava)
#install.packages('FSelector')
#library(FSelector)
librayr(mlbench)
#install.packages('rJava')
#library(rJava)
#install.packages('FSelector')
#library(FSelector)
library(mlbench)
weights <- chi.squared(params, data = train)
#install.packages('rJava')
#library(rJava)
#install.packages('FSelector')
#library(FSelector)
install.packages('mlbench')
library(mlbench)
weights <- chi.squared(params, data = train)
#install.packages('rJava')
install.packages("mlbench")
#library(rJava)
#install.packages('FSelector')
#library(FSelector)
install.packages('mlbench')
library(mlbench)
weights <- chi.squared(params, data = train)
?install.packages
# doesn't work
#install.packages('rJava')
#library(rJava)
# doesn't work
install.packages('FSelector', dependencies = TRUE)
library(FSelector)
# doesn't work
#install.packages('rJava')
#library(rJava)
# doesn't work
install.packages('FSelector', dependencies = TRUE)
library(FSelector)
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
# works but does not load chi.squared function
install.packages('mlbench')
library(mlbench)
weights <- chi.squared(params, data = train)
install.packages("mlbench")
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
# works but does not load chi.squared function
#install.packages('mlbench')
library(mlbench)
weights <- chi.squared(params, data = train)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
# works but does not load chi.squared function
#install.packages('mlbench')
library(mlbench)
weights <- chi.squared(params, data = train)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
weights <- chi.squared(params, data = train)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
#weights <- chi.squared(params, data = train)
#print(weights)
weights <- chisq.test(params, data = train)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
#weights <- chi.squared(params, data = train)
#print(weights)
weights <- chi.squared(params, data = train)
RStudio.version()
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
#weights <- chi.squared(params, data = train)
#print(weights)
weights <- chi.squared(params, data = train)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
install.packages('FSelector', dependencies = TRUE)
library(FSelector)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
# doesn't work
#weights <- chi.squared(params, data = train)
#print(weights)
head(meain)
head(main)
Sys.setenv(JAVA_HOME='C:\\Program Files\\Java\\jre7') # for 64-bit version
library(rJava)
#Sys.setenv(JAVA_HOME='C:\\Program Files\\Java\\jre7') # for 64-bit version
Sys.setenv(JAVA_HOME='C:\\Program Files (x86)\\Java\\jre7') # for 32-bit version
library(rJava)
#Sys.setenv(JAVA_HOME='C:\\Program Files\\Java\\jre7') # for 64-bit version
Sys.setenv(JAVA_HOME='C:\\Program Files (x86)\\Java\\jre7') # for 32-bit version
install.packages('rJava')
library(rJava)
Sys.setenv(JAVA_HOME='C:\\Program Files\\Java\\jre7') # for 64-bit version
#Sys.setenv(JAVA_HOME='C:\\Program Files (x86)\\Java\\jre7') # for 32-bit version
install.packages('rJava')
library(rJava)
as.table(as.matrix(main))
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#library(FSelector)
# doesn't work
weights <- chi.squared(params, data = train)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
library(FSelector)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
library(FSelector)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
install.packages("rJava",,"http://rforge.net/",type="source")
library(FSelector)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#install.packages('FSelector', dependencies = TRUE)
#install.packages("rJava",,"http://rforge.net/",type="source")
library(rJava)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
install.packages("rJava",,"http://cran.r-project.org",type="source")
library(rJava)
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
options(repos = 'http://cran.rstudio.com/')
#library(plyr)
#library(GGally)
#library(rattle)
#library(pROC)
#library(ROCR)
#library(iterators)
#library(rattle)
#library(kernlab)
#library(randomForest)   #For applying Random Forest
#library(e1071)          #For SVM
#library(rpart)          #For tree models
#library(rpart.plot)     #for plotting tree
#library(caTools)        #For calculating AUC
#library(readr)          #Fore reading data
#library(caret)
library(readxl)
library(stringr)
csv_path <- '/Users/jarad/Fake Folder/CSP/Recurring/Fraud and Chargeback Report/CSVs/Fraud Detection Data for R.xlsx'
super_main <- read_excel(csv_path)
colnames(super_main) <- str_replace_all(colnames(super_main), ' ', '_')
cols <- colnames(super_main)
for (c in cols){
if (class(super_main[[c]]) == 'character'){
super_main[[c]][is.na(super_main[[c]])] <- 'none'
}
}
library(tidyverse)
ggplot(super_main, aes(x = order_subtotal, color = fraud)) +
geom_density() +
xlim(-25,500) +
ggtitle('Density Plot for Order Totals per Fraud Group')
main <- super_main
do_this <- 'no'
d <- seq(max(super_main$date_purchased), length = 2, by = '-3 months')[-1]
d <- toString(d)
three_months_ago <- paste(substring(d, 1, 7), '-01', sep = '')
if (do_this == 'yes'){
main <- subset(main, date_purchased > three_months_ago)
}
ls <- list('date_purchased',
'orders_id',
'customers_id',
'customers_email_address',
'billing_street_address',
'billing_state',
'billing_country',
'billing_postcode',
'delivery_street_address',
'delivery_state',
'delivery_country',
'delivery_postcode',
'delivery_phone',
'ip_address',
'orders_status_name',
'year_and_month_purchased',
'order_subtotal_bins',
'email_domain')
main <- main[, -which(colnames(main) %in% ls)]
for (c in colnames(main)){
if (class(main[[c]]) == 'character'){
main[[c]] <- factor(main[[c]])
}
}
do_this <- 'no'
values <- main$order_subtotal
m <-mean(values)
s <- sd(values)
z <- (values - m)/s
if (do_this == 'yes'){
main$order_subtotal_z <- z
main <- subset(main, select = -c(order_subtotal))
}
for_cor <- main
for_cor[] <- lapply(main, as.integer)
cor_table <- cor(for_cor)
print(abs(cor_table) > 0.8)
# "fraud_before" and "fraud" are highly correlated; remove "fraud_before"
main <- main[, colnames(main) != 'fraud_before']
summary(main$fraud)
library(ROSE)
choice <- 'over'
main2 <- ovun.sample(fraud~.,
data = main,
method = choice,
p = 0.5,
seed = 1)$data
summary(main2$fraud)
ix <- sample(nrow(main2), round(nrow(main2) * 0.80), replace = FALSE)
train <- main2[ix,]
test <- main2[-ix,]
params <- fraud~.
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
library(tidyverse)
ggplot(super_main, aes(x = order_subtotal, color = fraud)) +
geom_density() +
xlim(-25,500) +
ggtitle('Density Plot for Order Subtotals per Fraud Group')
# doesn't work
#install.packages('rJava', dependencies = TRUE)
library(rJava)
options(repos = 'http://cran.rstudio.com/')
#library(plyr)
#library(GGally)
#library(rattle)
#library(pROC)
#library(ROCR)
#library(iterators)
#library(rattle)
#library(kernlab)
#library(randomForest)   #For applying Random Forest
#library(e1071)          #For SVM
#library(rpart)          #For tree models
#library(rpart.plot)     #for plotting tree
#library(caTools)        #For calculating AUC
#library(readr)          #Fore reading data
#library(caret)
library(readxl)
library(stringr)
csv_path <- '/Users/jarad/Fake Folder/CSP/Recurring/Fraud and Chargeback Report/CSVs/Fraud Detection Data for R.xlsx'
super_main <- read_excel(csv_path)
colnames(super_main) <- str_replace_all(colnames(super_main), ' ', '_')
cols <- colnames(super_main)
for (c in cols){
if (class(super_main[[c]]) == 'character'){
super_main[[c]][is.na(super_main[[c]])] <- 'none'
}
}
library(tidyverse)
ggplot(super_main, aes(x = order_subtotal, color = fraud)) +
geom_density() +
xlim(-25,500) +
ggtitle('Density Plot for Order Subtotals per Fraud Group')
main <- super_main
do_this <- 'no'
d <- seq(max(super_main$date_purchased), length = 2, by = '-3 months')[-1]
d <- toString(d)
three_months_ago <- paste(substring(d, 1, 7), '-01', sep = '')
if (do_this == 'yes'){
main <- subset(main, date_purchased > three_months_ago)
}
ls <- list('date_purchased',
'orders_id',
'customers_id',
'customers_email_address',
'billing_street_address',
'billing_state',
'billing_country',
'billing_postcode',
'delivery_street_address',
'delivery_state',
'delivery_country',
'delivery_postcode',
'delivery_phone',
'ip_address',
'orders_status_name',
'year_and_month_purchased',
'order_subtotal_bins',
'email_domain')
main <- main[, -which(colnames(main) %in% ls)]
for (c in colnames(main)){
if (class(main[[c]]) == 'character'){
main[[c]] <- factor(main[[c]])
}
}
do_this <- 'no'
values <- main$order_subtotal
m <-mean(values)
s <- sd(values)
z <- (values - m)/s
if (do_this == 'yes'){
main$order_subtotal_z <- z
main <- subset(main, select = -c(order_subtotal))
}
for_cor <- main
for_cor[] <- lapply(main, as.integer)
cor_table <- cor(for_cor)
print(abs(cor_table) > 0.8)
# "fraud_before" and "fraud" are highly correlated; remove "fraud_before"
main <- main[, colnames(main) != 'fraud_before']
summary(main$fraud)
library(ROSE)
choice <- 'over'
main2 <- ovun.sample(fraud~.,
data = main,
method = choice,
p = 0.5,
seed = 1)$data
summary(main2$fraud)
ix <- sample(nrow(main2), round(nrow(main2) * 0.80), replace = FALSE)
train <- main2[ix,]
test <- main2[-ix,]
params <- fraud~.
# doesn't work
#install.packages('rJava', dependencies = TRUE)
library(rJava)
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
options(repos = 'http://cran.rstudio.com/')
#library(plyr)
#library(GGally)
#library(rattle)
#library(pROC)
#library(ROCR)
#library(iterators)
#library(rattle)
#library(kernlab)
#library(randomForest)   #For applying Random Forest
#library(e1071)          #For SVM
#library(rpart)          #For tree models
#library(rpart.plot)     #for plotting tree
#library(caTools)        #For calculating AUC
#library(readr)          #Fore reading data
#library(caret)
library(readxl)
library(stringr)
csv_path <- '/Users/jarad/Fake Folder/CSP/Recurring/Fraud and Chargeback Report/CSVs/Fraud Detection Data for R.xlsx'
super_main <- read_excel(csv_path)
colnames(super_main) <- str_replace_all(colnames(super_main), ' ', '_')
cols <- colnames(super_main)
for (c in cols){
if (class(super_main[[c]]) == 'character'){
super_main[[c]][is.na(super_main[[c]])] <- 'none'
}
}
library(tidyverse)
ggplot(super_main, aes(x = order_subtotal, color = fraud)) +
geom_density() +
xlim(-25,500) +
ggtitle('Density Plot for Order Subtotals per Fraud Group')
main <- super_main
do_this <- 'no'
d <- seq(max(super_main$date_purchased), length = 2, by = '-3 months')[-1]
d <- toString(d)
three_months_ago <- paste(substring(d, 1, 7), '-01', sep = '')
if (do_this == 'yes'){
main <- subset(main, date_purchased > three_months_ago)
}
ls <- list('date_purchased',
'orders_id',
'customers_id',
'customers_email_address',
'billing_street_address',
'billing_state',
'billing_country',
'billing_postcode',
'delivery_street_address',
'delivery_state',
'delivery_country',
'delivery_postcode',
'delivery_phone',
'ip_address',
'orders_status_name',
'year_and_month_purchased',
'order_subtotal_bins',
'email_domain')
main <- main[, -which(colnames(main) %in% ls)]
for (c in colnames(main)){
if (class(main[[c]]) == 'character'){
main[[c]] <- factor(main[[c]])
}
}
do_this <- 'no'
values <- main$order_subtotal
m <-mean(values)
s <- sd(values)
z <- (values - m)/s
if (do_this == 'yes'){
main$order_subtotal_z <- z
main <- subset(main, select = -c(order_subtotal))
}
for_cor <- main
for_cor[] <- lapply(main, as.integer)
cor_table <- cor(for_cor)
print(abs(cor_table) > 0.8)
# "fraud_before" and "fraud" are highly correlated; remove "fraud_before"
main <- main[, colnames(main) != 'fraud_before']
summary(main$fraud)
library(ROSE)
choice <- 'over'
main2 <- ovun.sample(fraud~.,
data = main,
method = choice,
p = 0.5,
seed = 1)$data
summary(main2$fraud)
ix <- sample(nrow(main2), round(nrow(main2) * 0.80), replace = FALSE)
train <- main2[ix,]
test <- main2[-ix,]
params <- fraud~.
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
.rs.restartR()
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
# doesn't work
install.packages('rJava', dependencies = TRUE)
library(rJava)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
options("java.home"="/Library/Java/JavaVirtualMachines/jdk-9.0.4.jdk/Contents/Home/lib")
Sys.setenv(LD_LIBRARY_PATH='$JAVA_HOME/server')
dyn.load('/Library/Java/JavaVirtualMachines/jdk-9.0.4.jdk/Contents/Home/lib/server/libjvm.dylib')
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#options("java.home"="/Library/Java/JavaVirtualMachines/jdk-9.0.4.jdk/Contents/Home/lib")
#Sys.setenv(LD_LIBRARY_PATH='$JAVA_HOME/server')
#dyn.load('/Library/Java/JavaVirtualMachines/jdk-9.0.4.jdk/Contents/Home/lib/server/libjvm.dylib')
library(rJava)
# doesn't work
#install.packages('rJava', dependencies = TRUE)
#library(rJava)
# doesn't work
#options("java.home"="/Library/Java/JavaVirtualMachines/jdk-9.0.4.jdk/Contents/Home/lib")
#Sys.setenv(LD_LIBRARY_PATH='$JAVA_HOME/server')
#dyn.load('/Library/Java/JavaVirtualMachines/jdk-9.0.4.jdk/Contents/Home/lib/server/libjvm.dylib')
library(rJava)
